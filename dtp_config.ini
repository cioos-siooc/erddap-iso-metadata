[driver]
driver_type = sharepoint

[static_data]
mcf_template = metadata_skeleton_sa.yml

# list of optional and recommended variables to be added to the resulting metadata profile
# REQUIRED VARIABLES          VARIABLE #N 
#                             (reference and/or rename by using var:<name> or var:<in_name>:<out_name>)
#   - title *used               - sourceName
#   - summary *used             - destinationName
#   - creator_name *used        - long_name
#   - creator_type              - standard_name
#   - creator_email *used       - dataType
#   - institution *used         - _FillValue
#   - infoUrl                   - units
#   - license                   - range (min/max)
#   - cdm_data_type *used       - ioos_category
#                               - comment 
# Default list: 
# opt_rec_variables = "
# acknowledgement, 
# history, 
# id, 
# naming_authority, 
# product_version, 
# references, 
# comment"
# convert using or:<in_name>:<out_name>
opt_rec_variables = acknowledgement, comment, contributor_name, contributor_role, coverage_content_type, creator_email, creator_name, creator_type, creator_url, data_maintenance_note, date_created, date_issued, date_modified, distributor_name, geospatial_bounds, geospatial_lat_resolution, geospatial_lat_units, geospatial_lon_resolution, geospatial_lon_units, geospatial_vertical_max, geospatial_vertical_min, geospatial_vertical_positive, geospatial_vertical_resolution, geospatial_vertical_units, history, id, institution, keywords, keywords_fra, keywords_vocabulary, language, language_country, license, naming_authority, platform_id, platform_id_authority, platform_name, platform_role, processing_level, progress_code, product_version, project, project, publisher_country, publisher_email, publisher_institution, publisher_name, publisher_type, publisher_url, scope_code, references, standard_name_vocabulary, time_coverage_duration, time_coverage_end, time_coverage_resolution, time_coverage_start, use_constraints

[dynamic_data]
erddap_server = https://www.smartatlantic.ca/erddap/
#erddap_server = http://belafonte.ocean.dal.ca:8080/erddap

erddap_protocol = tabledap

# metadata-xml seems to want this for the
# dataset url, not sure why but I'd like to 
# be able to change it.  Until that is 
# viable, here's a setting.
dataset_url_field = erddap_dataset_url

[output]
# where to output yaml and ISO xml profiles 
# (relative to dtp.py script directory).
target_dir = ../erddap-iso-metadata/sa_platforms/test

# working directory for XML translation process
working_dir = ../metadata-xml

# Fileoutput format relative to target_dir path. 
# First field is station name, the other is file format
output_format = sma_ISO-CIOOS_%%s.%%s

# target python script to run after
# First field is path to YAML source file.
#profile_generator = -m metadata_xml -f %%s
profile_generator = %%s

# (OPTIONAL) XML file output is determined by generator, 
# additional arguments will be passed if 
# present after the YAML source file path
additional_arguments = --ignore_validation_errors -o ../erddap-iso-metadata/sa_platforms/test

# executable prefix, python assumed, use absolute path if executable 
# is not findable via environment path
#profile_language = python
profile_language = C:/temp/CIOOS/erddap-iso-metadata/env/Scripts/python.exe -m metadata_xml -f 

# (OPTIONAL) A script or other executable that should be run 
# between the initial YAML file generation and XML translation
#pre_processor = python station_position_filter.py --lat_adjust 0.01
pre_processor = C:/temp/CIOOS/erddap-iso-metadata/env/Scripts/python.exe station_position_filter.py --lat_adjust 0.01 --lon_adjust 0.01

# (OPTIONAL) A script or other executable that should be run 
# AFTER the XML translation
post_processor = 

# 1. WE SHOULD SUPPLY A CIOOS CUSTOMIZED CONFIGURATION OF ERDDAP AS A DOCKER IMAGE.
# 2. CUSTOMIZE THE DATASET SUBMISSION FORM FOR CIOOS SUPPLY LIST CIOOS EOVs CATEGORIES 
# pscp -P 22 -i "Y:\CIOOS\Phase 2\PuttyPrivateKey - arbutus.ppk" *.xml sbruce@dev.cioosatlantic.ca:/srv/metadata/marine_institute/
